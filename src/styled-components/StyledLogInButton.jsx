import { styled, Button } from "@mui/material";

export const StyledLgInButton = styled(Button)({
  padding: "4px 28px",
  color: "#FFFFFF",
  borderColor: "#FFFFFF",
  borderWidth: "2px",
  textTransform: "none",
  lineHeight: '30px',
  "&:hover": {
    borderColor: "#FFFFFF",
    borderWidth: "2px",
  },
});
