import { styled, Button } from "@mui/material";

export const StyledLgInButton = styled(Button)({
  padding: '6px 26px',
  color: '#FFFFFF',
  borderColor: '#FFFFFF',
  borderWidth: '2px',
  textTransform: 'none',
  '&:hover': {
    borderColor: '#FFFFFF',
    borderWidth: '2px'
  }
});
